---
phase: 03-auth-config-hardening
plan: 03
type: execute
wave: 3
depends_on: ["01", "02"]
files_modified:
  - src/cli.mjs
  - test/auth-cli.test.mjs
autonomous: true
requirements: [CLI-02, CLI-07]
tags: [auth, cli, logout, whoami, profiles]

must_haves:
  truths:
    - "appo logout calls DELETE /api/v1/user/tokens/current and clears the local token even when revoke fails (401/network)"
    - "appo whoami reports the active env + api_base + liveness (N apps) and never prints the token"
    - "appo env list marks the active profile and prints no token; appo env use <name> sets current"
    - "--env <name> selects the profile for token + api_base for a single invocation, overriding current"
    - "appo login --token <pat> authenticates headless; the device flow remains the default interactive path"
  artifacts:
    - path: "src/cli.mjs"
      provides: "env threaded once in run(); logout revoke+finally-clear; whoami env/api_base/liveness; case 'env' (list/use); login --token branch; --env wiring; USAGE updates"
      contains: "/api/v1/user/tokens/current"
    - path: "test/auth-cli.test.mjs"
      provides: "Contract coverage of the verb surface: logout 204/401/network, whoami 200/401, env list/use, --env override, login --token, PAT-never-printed"
      contains: "node:test"
  key_links:
    - from: "src/cli.mjs case 'logout'"
      to: "DELETE /api/v1/user/tokens/current then clearProfileToken(env) in finally"
      via: "revoke-then-always-clear (D-10/D-11)"
      pattern: "finally"
    - from: "src/cli.mjs run()"
      to: "activeProfileName(flags.env) threaded into resolveApiBase/apiFetch/auth verbs"
      via: "single env resolution (Pitfall 7)"
      pattern: "activeProfileName\\(flags\\.env\\)"
---

<objective>
Wire the CLI verb surface onto the env-aware config + transport from Plans 01-02: resolve the active
env once in `run()` and thread it everywhere; make `logout` revoke server-side
(`DELETE /api/v1/user/tokens/current`) and ALWAYS clear the local token in a `finally`; enrich `whoami`
to report active env + api_base + liveness (no token); add a `case 'env'` for `env list`/`env use`;
add the `login --token` branch ahead of the device flow; guard a value-less `--env`; and update USAGE
to document `--env`, `env list/use`, `login --token`, `APPO_TOKEN`/`APPO_ENV`, and the dashboard
PAT-mint path.

Purpose: completes CLI-02 (logout server-side revoke, multi-env selection, whoami) and CLI-07
(`login --token` ergonomics + the `APPO_TOKEN` documentation). This is the user-facing surface; it
consumes the helpers from Plans 01-02 and invents no new transport.

Output: an env-aware, revoke-capable `src/cli.mjs` and a new `test/auth-cli.test.mjs` contract suite.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-auth-config-hardening/03-RESEARCH.md
@.planning/phases/03-auth-config-hardening/03-PATTERNS.md
@.planning/phases/03-auth-config-hardening/03-01-SUMMARY.md
@.planning/phases/03-auth-config-hardening/03-02-SUMMARY.md
@src/cli.mjs
@test/destructive-verbs.test.mjs
@test/helpers/mockFetch.mjs

<interfaces>
<!-- From Plan 01 (config.mjs) and Plan 02 (api.mjs/login.mjs) — import and use; do NOT re-derive. -->
```javascript
// config.mjs
configPath()                        // { dir, file } resolved PER CALL from APPO_CONFIG_HOME (lazy — honors env set in beforeEach)
activeProfileName(flagEnv)          // --env > APPO_ENV > current > "default"
resolveApiBase(flagValue, env)      // --api > APPO_API_BASE > profile.api_base > default
storedToken(env)                    // APPO_TOKEN > profiles[env].token
readConfig()                        // { current, profiles:{ name:{ api_base, token } } }
clearProfileToken(env)              // logout: drop token, keep api_base
setCurrent(env)                     // env use
// api.mjs
apiFetch(apiBase, method, path, body, env)   // env-aware token + env-named 401
// login.mjs
login(apiBase, env)                          // device flow → writeProfile(env, ...)
loginWithToken(apiBase, env, pat)            // validate via GET /apps → store, or throw {status:401}
```

<!-- Backend contract (VERIFIED, 03-RESEARCH.md):
  DELETE /api/v1/user/tokens/current → 204 on success (apiFetch returns null);
                                       already-invalid token → 401 (auth middleware rejects pre-controller).
  GET    /api/v1/apps                → 200 { data:[...] } (whoami liveness + count). 401 on dead token.
  No /api/v1/user or /me endpoint exists — whoami reports NO identity (documented backend gap, D-12).
-->

<!-- Current cli.mjs anchors:
  run(): apiBase resolved at line 271 (`const apiBase = resolveApiBase(flags.api);`);
         `const [command, sub, ...rest] = positional;` at line 272;
         value-less `--api` guard at lines 266-269 (the template for a `--env` guard).
  case 'login'  lines 276-280 (device only — add --token branch BEFORE it).
  case 'logout' lines 282-285 (clearConfig() local-only — replace with revoke+finally-clear).
  case 'whoami' lines 287-297 (cfg.token guard + GET /apps liveness — map to storedToken(env), enrich).
  USAGE lines 7-50; aligned `line(k,v)` idiom at line 93. confirmGate/renderError exported line 254.
-->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Failing contract suite for the auth verb surface (RED)</name>
  <files>test/auth-cli.test.mjs</files>
  <read_first>
    - `test/destructive-verbs.test.mjs:1-90` — harness: `import { run } from '../src/cli.mjs';`, `captureLog`/`captureAll`/`silentRun` helpers, `afterEach(resetMockFetch)`, `const API = ['--api','http://test.local'];`, `lastRequest()`/`requests` assertions, `assert.equal(requests.length, 0)` no-write idiom (line 63). NOTE: `captureAll` (lines 26-39) intercepts ONLY `console.log` and `console.error` — anything emitted via `console.warn` is NOT captured.
    - `test/helpers/mockFetch.mjs:29-59` (FIFO + recorded headers), `:76-81` (`stubToken`, lazy path via `configPath()`).
    - 03-RESEARCH.md Code Examples: logout Pattern 5 (lines 274-290), whoami (lines 405-423), `env list`/`use` (lines 427-447), `login --token` (lines 380-401).
    - 03-RESEARCH.md "Validation Architecture" lines 520-538 (the requirement→test rows for logout/whoami/env/`--env`/PAT-never-printed).
    - Plan 01 §"Lazy config path seam" — config.mjs resolves its path per call via `configPath()`; set `APPO_CONFIG_HOME` in `beforeEach`, NOT "before import".
  </read_first>
  <behavior>
    - logout 204 (D-10): seed a token for env `default`; stub `{status:204, body:null}`; `run(['logout', ...API])` → exit 0; `lastRequest().method === 'DELETE'` and `lastRequest().path` matches `/api/v1/user/tokens/current`; afterwards `storedToken('default')` is null (local token cleared); a success line mentioning the env was printed.
    - logout 401/already-invalid (D-11, Pitfall 3): stub `{status:401, body:{error:'unauthorized'}}`; `run(['logout',...API])` → exit 0; `storedToken('default')` STILL null (cleared anyway); a WARNING was printed — captured via `captureAll` (the warning is emitted with `console.error`, which `captureAll` intercepts) and it contains "Could not confirm" (or similar) and NO token substring.
    - logout network error (D-11): make the stub throw (e.g. queue nothing / installMockFetch that rejects) → exit 0; token still cleared + warning (again via `console.error`, captured by `captureAll`).
    - logout per-env (D-11): seed tokens for `default` and `staging`; `run(['logout','--env','staging',...API])` with a 204 stub → `storedToken('staging')` null but `storedToken('default')` UNCHANGED (siblings untouched).
    - whoami 200 (D-12): seed token; stub `{status:200, body:{data:[{id:1},{id:2}]}}`; `run(['whoami',...API])` → exit 0; output contains the env name and the api_base and `2 app(s)`; output contains NO token substring.
    - whoami 401: stub `{status:401}`; `run(['whoami',...API])` → exit 1; output names the env + `appo login`; no token printed.
    - whoami no token: with no stored token and no APPO_TOKEN, `run(['whoami',...API])` → exit 1 with a "run `appo login`" message (no network call: `requests.length === 0`).
    - env list (D-04/D-13, Pitfall 5): seed two profiles with tokens + api_bases, `current` set to one; `run(['env','list'])` → exit 0; output contains both profile names, marks the current one, and contains NEITHER token string.
    - env use (D-04): with profile `staging` present, `run(['env','use','staging'])` → exit 0 and `readConfig().current === 'staging'`; `run(['env','use','nope'])` (unknown) → exit 2; `run(['env','use'])` (missing name) → exit 2.
    - --env override (Pitfall 7): seed `current='production'` with token `prod-tok`+api_base `http://prod.local`, and `staging` token `stg-tok`+api_base `http://stg.local`; stub a 200; `run(['whoami','--env','staging'])` → the request's `Authorization` is `Bearer stg-tok` AND the URL used the staging api_base (assert via `lastRequest().url` containing `stg.local`), NOT production.
    - login --token 200 (D-07): stub `{status:200, body:{data:[]}}`; `run(['login','--token','good-pat',...API])` → exit 0; `storedToken('default') === 'good-pat'`; no captured output contains `good-pat`.
    - login --token 401 (D-07, Pitfall 6): stub `{status:401}`; `run(['login','--token','bad-pat',...API])` → exit 1; nothing stored (`storedToken('default')` unchanged/null); `requests.length === 1` (only the probe); no output contains `bad-pat`.
    - value-less --env: `run(['whoami','--env'])` (no value) → exit 2 usage error.
    - PAT-never-printed sweep: across the logout/whoami/env-list/login--token captures (using `captureAll`), assert none contains any seeded token string (`prod-tok`/`stg-tok`/`good-pat`/`bad-pat`).
  </behavior>
  <action>
Create `test/auth-cli.test.mjs` copying the `test/destructive-verbs.test.mjs` harness wholesale
(`run` from cli.mjs, `installMockFetch`/`resetMockFetch`/`lastRequest`/`requests`/`stubToken`,
`afterEach(resetMockFetch)`, the `captureAll`/`silentRun` log-capture helpers, `const API`). Use
`captureAll` (not `captureLog`) for the logout warning and PAT-leak assertions — the logout warning is
emitted via `console.error` (consistent with the existing usage-guard stderr idiom), and `captureAll`
intercepts both `console.log` and `console.error`. Do NOT rely on `console.warn` being captured (the
copied harness ignores it).

Config isolation (LAZY-path aware — do NOT set the env "before import"; ESM hoists imports). In
`beforeEach`, set `process.env.APPO_CONFIG_HOME = mkdtempSync(join(tmpdir(),'appo-cli-'))`; config.mjs
resolves its path per call via `configPath()`, so this is honored on the next read/write. In `afterEach`,
capture the temp dir, then `delete process.env.APPO_TOKEN/APPO_ENV/APPO_API_BASE/APPO_CONFIG_HOME` and
`rmSync(dir,{recursive:true,force:true})`. Seed profile shapes via `writeProfile`/`writeConfig` (import
from config.mjs) and `storedToken`/`readConfig` for assertions.

For the network-error logout case, install a fetch stub that throws (a one-off
`globalThis.fetch = async () => { throw new Error('network'); }` inside the test, restored by
`resetMockFetch`/afterEach). For multi-call verbs queue one canned response per call (FIFO).

Write one `test(...)` per <behavior> bullet. These FAIL now: logout still does local-only
`clearConfig`, whoami doesn't name the env, `case 'env'` and the `--token`/`--env` wiring don't exist.
Confirm RED before Task 2.
  </action>
  <verify>
    <automated>node --test test/auth-cli.test.mjs 2>&1 | grep -E "# (tests|fail)"</automated>
  </verify>
  <acceptance_criteria>
    - `test/auth-cli.test.mjs` exists; imports `run` from `../src/cli.mjs` and config helpers.
    - `grep -c "process.env.APPO_CONFIG_HOME" test/auth-cli.test.mjs` ≥ 1, set inside a `beforeEach` (not a top-level statement racing ESM import hoisting).
    - `grep "/api/v1/user/tokens/current" test/auth-cli.test.mjs` matches (logout revoke assertion present).
    - `grep -E "'--env','staging'" test/auth-cli.test.mjs` matches (override case present).
    - The logout-failure tests assert the warning via the `captureAll` lines (console.error path), NOT via a `console.warn` capture.
    - `node --test test/auth-cli.test.mjs` is RED at this point.
  </acceptance_criteria>
  <done>The contract suite encodes all <behavior> bullets, isolates config via APPO_CONFIG_HOME in beforeEach (lazy path), captures the logout warning through captureAll (console.error), and fails against the current cli.mjs.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire the auth verb surface in src/cli.mjs (GREEN)</name>
  <files>src/cli.mjs</files>
  <read_first>
    - `src/cli.mjs` lines 1-5 (imports), 256-298 (run head + login/logout/whoami cases), 7-50 (USAGE), 91-102 (`line(k,v)` idiom), 264-272 (value-less-flag guard + apiBase resolution + positional destructure), 254 (exports).
    - 03-RESEARCH.md Pattern 5 (logout, lines 274-290), Code Examples whoami (405-423) and env (427-447) and login --token (380-401).
    - 03-PATTERNS.md "src/cli.mjs" lines 124-167 (per-case map: thread env once; login --token branch; logout revoke+finally; whoami map; case 'env'; USAGE additions; value-less guard idiom).
    - 03-RESEARCH.md "How a user mints a PAT" lines 139-142 (the dashboard mint note for USAGE).
    - Existing stderr idiom: the usage guards already use `console.error` (e.g. lines 266-269). The logout revoke-failure warning MUST use `console.error` too (the test harness's `captureAll` intercepts console.log/console.error only — `console.warn` would not be captured and the assertion would fail).
  </read_first>
  <action>
Edit `src/cli.mjs`. Import the new helpers: from `./config.mjs` add
`activeProfileName, storedToken, clearProfileToken, setCurrent, writeProfile` (keep `resolveApiBase`,
drop `clearConfig` if no longer used — logout no longer calls it); from `./login.mjs` add
`loginWithToken` alongside `login`.

1) **Value-less `--env` guard.** After the existing `if (flags.api === true)` block (lines 266-269),
   add the mirror: `if (flags.env === true) { console.error('Usage: --env <name> requires a value'); return 2; }`
   Same for `--token`: `if (flags.token === true) { console.error('Usage: --token <pat> requires a value'); return 2; }`

2) **Thread env once (Pitfall 7).** Replace line 271 with:
   `const env = activeProfileName(flags.env); const apiBase = resolveApiBase(flags.api, env);`
   Every authed `apiFetch(apiBase, METHOD, path, body)` call in `run()` gains `env` as the 5th arg.
   (Mechanically: add `, env` to the existing apiFetch calls in login/logout/whoami/apps/status/etc. —
   the optional 5th param is harmless on read verbs and makes `--env` correct for all of them.)

3) **`case 'login'` — add `--token` branch BEFORE the device call.**
   ```javascript
   case 'login': {
     if (typeof flags.token === 'string' && flags.token) {
       try {
         await loginWithToken(apiBase, env, flags.token);   // validate via GET /apps, store on 200
       } catch (err) {
         if (err.status === 401) { console.error(`Token rejected by ${apiBase} — not stored.`); return 1; }
         throw err;                                          // network/other → renderError
       }
       console.log(`Stored token for env '${env}' (${apiBase}).`);   // NEVER echo flags.token
       return 0;
     }
     const { apiBase: base } = await login(apiBase, env);
     console.log(`\n  Authenticated env '${env}'. Connected to ${base}.\n`);
     return 0;
   }
   ```

4) **`case 'logout'` — revoke-then-always-clear (D-10/D-11, Pattern 5).** Replace the local-only body.
   The revoke-failure WARNING is emitted with `console.error` (NOT `console.warn`) — this matches the
   existing usage-guard stderr idiom and is the channel the test harness's `captureAll` intercepts:
   ```javascript
   case 'logout': {
     try {
       await apiFetch(apiBase, 'DELETE', '/api/v1/user/tokens/current', null, env);   // 204 → null
       console.log(`Logged out of '${env}' — token revoked server-side and cleared.`);
     } catch (err) {
       console.error(`Could not confirm server-side revocation for '${env}' (${err.message}). Clearing local token anyway.`);
     } finally {
       clearProfileToken(env);   // load-bearing: NEVER leave a stale token (Pitfall 3)
     }
     return 0;
   }
   ```
   The `finally` clear is the high-severity invariant. `clearProfileToken` (not `clearConfig`) so sibling
   profiles are untouched. The warning must not contain any token substring.

5) **`case 'whoami'` — env + api_base + liveness (D-12).** Map the token guard to `storedToken(env)`;
   on missing token print `` `No token for env '${env}'. Run \`appo login\`.` `` and `return 1` (no
   network call). Else `GET /api/v1/apps` (with `env`), and on 200 print, using the aligned `line(k,v)`
   idiom: `env`, `api_base` (= apiBase), `status: authenticated — N app(s)`. NO identity field (backend
   gap, D-12). Token NEVER printed. On `err.status === 401` print
   `` `env '${env}': token rejected — run \`appo login\`.` `` and `return 1`; rethrow other errors.

6) **New `case 'env'` (D-04/D-13).** `sub`/`rest` are already destructured.
   - `env list` (or `sub === undefined`): iterate `readConfig().profiles`; for each
     `console.log(\`  ${name===cfg.current?'*':' '} ${name.padEnd(16)} ${p.api_base ?? '(default)'}\`)`.
     NEVER print `p.token` (Pitfall 5). `return 0`.
   - `env use`: `const name = rest[0];` if missing → `console.error('Usage: appo env use <name>'); return 2;`
     if `!cfg.profiles[name]` → `console.error(\`No such env '${name}'. Run \\\`appo login --env ${name}\\\` first.\`); return 2;`
     else `setCurrent(name); console.log(\`Active env: ${name}.\`); return 0;`
   - unknown sub → `console.error(\`Unknown env subcommand: ${sub}\`); return 2;`

7) **USAGE (lines 7-50).** In the Auth section add:
   `appo login --token <pat>     Authenticate non-interactively with a dashboard PAT`,
   `appo env list                List configured environments`,
   `appo env use <name>          Switch the active environment`.
   In Options add a `--env <name>   Select the environment/profile (env: APPO_ENV)` line (mirror the
   `--api` line). Add an env-vars note documenting `APPO_TOKEN` (ephemeral, highest precedence, never
   stored) and `APPO_ENV`, plus the mint path: "Create a PAT in the dashboard, then
   `appo login --token <pat>` or set APPO_TOKEN." Keep the voice neutral (CLAUDE.md). Update the whoami
   line to mention it shows the active environment.

Run Task 1's suite to GREEN.
  </action>
  <verify>
    <automated>node --test test/auth-cli.test.mjs 2>&1 | grep -E "# (tests|pass|fail)"</automated>
  </verify>
  <acceptance_criteria>
    - `grep "DELETE', '/api/v1/user/tokens/current'" src/cli.mjs` matches; `grep -n "finally" src/cli.mjs` shows the logout finally-clear; `grep "clearProfileToken(env)" src/cli.mjs` matches.
    - The logout catch block emits the warning via `console.error` (`grep -n "Could not confirm" src/cli.mjs` is on a `console.error(...)` line); `grep "console.warn" src/cli.mjs` returns nothing for the logout case.
    - `grep "clearConfig" src/cli.mjs` returns nothing in the logout case (replaced).
    - `grep -E "case 'env'" src/cli.mjs` matches and `grep "setCurrent(" src/cli.mjs` matches.
    - `grep "loginWithToken(apiBase, env" src/cli.mjs` matches; `grep -nE "console\\.(log|error).*flags\\.token" src/cli.mjs` returns nothing (PAT never echoed).
    - `grep "activeProfileName(flags.env)" src/cli.mjs` matches (env threaded once).
    - `grep -E "login --token|env list|env use|--env <name>|APPO_TOKEN|APPO_ENV" src/cli.mjs` (USAGE) returns all entries.
    - `node --test test/auth-cli.test.mjs` → all tests pass (GREEN), including: logout clears on 204 AND on 401/network (warning captured via console.error); whoami names the env + count, no token; `--env staging` overrides current for token+base; `login --token` stores on 200 / refuses on 401 without echoing the PAT; `env list` prints no token; `env use` sets current.
  </acceptance_criteria>
  <done>cli.mjs threads the active env, revokes-then-always-clears on logout (warning via console.error), enriches whoami, adds env list/use and login --token, guards value-less --env/--token, and documents everything in USAGE; Task 1's suite is green.</done>
</task>

<task type="auto">
  <name>Task 3: Full-suite regression + help-text + PAT-leak sweep</name>
  <files>test/auth-cli.test.mjs</files>
  <read_first>
    - 03-VALIDATION.md lines 26-32 (verify with `npm test`; baseline 84) and lines 38-40 (smoke checks: `APPO_TOKEN=x appo whoami`, `appo env list`, `appo logout` issues DELETE).
    - `test/help.test.mjs` — the existing USAGE assertions; confirm the new USAGE lines don't break exact-match expectations (adjust the help test ONLY if it asserts the full block verbatim).
  </read_first>
  <action>
Run `npm test` (pins `--test-concurrency=1`). All prior suites (baseline 84 + config-profiles + auth)
plus the new `auth-cli` suite must be green. The `--env`/`--token` flags parse for free via the existing
`parseArgs`; the added 5th `env` arg on read-verb apiFetch calls is harmless.

If `test/help.test.mjs` asserts USAGE content, ensure it still passes — extend its expectations to
cover the new lines if it checks for presence of specific commands; do NOT weaken it.

PAT-leak sweep (T-03 high control): grep the full test output for any seeded token value to prove
nothing leaked:
`npm test 2>&1 | grep -E "good-pat|bad-pat|prod-tok|stg-tok|disk-tok|env-tok" && echo LEAK || echo CLEAN`
The expected result is `CLEAN` (no token string ever printed by any verb).

Confirm the test run left the real `~/.appo/config.json` untouched (lazy path + temp-dir isolation).
  </action>
  <verify>
    <automated>npm test 2>&1 | grep -E "# (tests|pass|fail)" && (npm test 2>&1 | grep -qE "good-pat|bad-pat|prod-tok|stg-tok|disk-tok|env-tok" && echo LEAK || echo CLEAN)</automated>
  </verify>
  <acceptance_criteria>
    - `npm test` reports `# fail 0`.
    - `npm test` `# pass` ≥ 84 + config-profiles + auth + auth-cli counts (all suites intact).
    - The token-leak sweep prints `CLEAN` — no seeded token string appears anywhere in the test output.
    - `test/help.test.mjs` passes (USAGE additions did not break it).
    - After the run, `cat ~/.appo/config.json 2>/dev/null` contains none of the seeded token strings.
  </acceptance_criteria>
  <done>`npm test` fully green across all suites; no PAT leaks in output; help text consistent; real config untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI verb → `/api/v1` (Bearer) | logout/whoami/login--token cross the network with the active-env PAT |
| user terminal ↔ config store | logout mutates (deletes) a stored credential; env use mutates `current`; the token must never appear in any printed output |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-11 | Repudiation / spoofing | `logout` server-side revoke | mitigate | `logout` issues `DELETE /api/v1/user/tokens/current` so the PAT is killed server-side, not just locally — a logged-out token cannot be replayed. Test asserts the DELETE is issued. HIGH — a local-only logout leaves a live PAT (the exact gap this phase closes). |
| T-03-12 | Information disclosure | `logout` failure path | mitigate | The local clear is in a `finally`, so a lost-device token is removed locally even when revoke returns 401/network-error. The failure warning is emitted via `console.error` (captured, auditable) and contains no token. Test asserts the token is gone on every branch. HIGH. |
| T-03-13 | Information disclosure | `whoami` / `env list` output | mitigate | Neither verb ever prints a token — whoami shows env/api_base/count via `line(k,v)`, env list shows name/api_base only. Tests assert no token substring in captured output. HIGH. |
| T-03-14 | Information disclosure | `login --token` echo | mitigate | The success/refusal lines name the env/api_base, never `flags.token`. The full-suite PAT-leak sweep proves no seeded token appears in any output. HIGH. |
| T-03-15 | Tampering | `--env` mis-selection | mitigate | `env` is resolved once in `run()` and threaded to resolveApiBase + apiFetch, so a verb cannot silently act on the wrong profile (Pitfall 7). Test asserts `--env staging` uses the staging token+base over `current=production`. |
| T-03-16 | Elevation of privilege | `env use` of an unknown profile | accept | `env use nope` returns a usage error (exit 2) and does not create a profile — selection cannot fabricate credentials. Low risk; surface only. |
</threat_model>

<verification>
- `npm test` green across all suites; baseline 84 intact.
- `logout` issues `DELETE /api/v1/user/tokens/current` and clears the local token on 204, 401, and network-error — always (finally), per-env; failure warning via `console.error`.
- `whoami` reports env + api_base + liveness (N apps); no token printed; 401 → re-login path.
- `env list` marks the active profile, prints no token; `env use <name>` sets current; unknown/missing → exit 2.
- `--env staging` overrides `current` for one invocation's token + api_base.
- `login --token` stores on 200, refuses on 401, never echoes the PAT; device flow remains default.
- USAGE documents `--env`, `env list/use`, `login --token`, `APPO_TOKEN`/`APPO_ENV`, and the dashboard mint path.
- Token-leak sweep across test output is CLEAN.
</verification>

<success_criteria>
- `case 'logout'` revokes server-side then clears local in a `finally`, warning on failure via `console.error` (D-10/D-11).
- `case 'whoami'` reports active env + api_base + liveness, no token (D-12).
- `case 'env'` provides list (active-marked, token-free) and use (sets current) (D-04).
- `case 'login'` has a validate-then-store `--token` branch ahead of the device flow (D-07); device flow writes into the active profile.
- `--env` resolved once and threaded; value-less `--env`/`--token` → exit 2.
- USAGE updated; full suite green; no PAT leaks.
</success_criteria>

<output>
After completion, create `.planning/phases/03-auth-config-hardening/03-03-SUMMARY.md`
</output>
</output>
