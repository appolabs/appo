---
phase: 06-packaging-docs-release
plan: 03
type: execute
wave: 2
depends_on: [06-01, 06-02]
files_modified:
  - README.md
  - llms.txt
  - test/integration/docs.test.mjs
autonomous: true
requirements: [CLI-05]
must_haves:
  truths:
    - "README.md documents the full command surface — every verb incl. `appo ship`, `appo init`, `appo upgrade`, `--version` — with a ship-first quickstart, env vars, exit codes, profiles, and CI auth"
    - "README.md contains a RELEASING runbook: the user's one-time npm trusted-publisher registration + first manual publish (D-09)"
    - "llms.txt follows the SDK shape (`# @appolabs/appo` title, `> ` tagline, `## ` sections) and links to a README anchor for every command"
    - "Every command in the authoritative inventory appears in BOTH README.md and llms.txt (asserted by a grep test)"
  artifacts:
    - path: "README.md"
      provides: "full-surface CLI documentation (rewritten, not appended)"
      contains: "## Ship"
      min_lines: 120
    - path: "llms.txt"
      provides: "agent-facing condensed doc linking into README anchors"
      contains: "# @appolabs/appo"
    - path: "test/integration/docs.test.mjs"
      provides: "command-coverage + llms.txt shape assertions over README + llms.txt"
      min_lines: 25
  key_links:
    - from: "llms.txt"
      to: "README.md anchors"
      via: "(README.md#anchor) markdown links"
      pattern: "README.md#"
    - from: "README.md / llms.txt"
      to: "the command inventory"
      via: "every verb documented"
      pattern: "appo ship"
---

<objective>
Rewrite `README.md` to document the complete v0.1 CLI surface — ship-first quickstart, every verb (incl.
the new `init`/`upgrade`/`--version` from Plan 01), env vars, exit codes, multi-environment profiles, and
non-interactive/CI auth — and add the RELEASING runbook for the user's one-time trusted-publisher setup
and first manual publish (D-09). Generate `llms.txt` in the SDK shape, linking into the README anchors for
every command. A grep test asserts both docs cover the full inventory.

Purpose: Satisfies CLI-05 SC4 (README + command reference + llms.txt covering every command incl. ship).
Output: rewritten `README.md`, new `llms.txt`, new `test/integration/docs.test.mjs`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-packaging-docs-release/06-RESEARCH.md
@.planning/phases/06-packaging-docs-release/06-PATTERNS.md

<interfaces>
Authoritative command inventory (06-RESEARCH.md lines 508-527) — EVERY item must appear in README AND llms.txt:
- Auth: `login` (device), `login --token <pat>`, `logout`, `whoami`
- Environments: `env list`, `env use <name>`
- Apps: `apps create`, `apps list`, `apps show <id>`, `apps set-name <id> <name>`
- Lifecycle: `ship`, `status`, `build`, `configure`, `rejection`, `fix-recipe`, `publish`, `push`, `resubmit`
- Packaging (new this phase, from Plan 01): `init`, `upgrade`, `--version`/`-v`
- Env vars: `APPO_TOKEN`, `APPO_ENV`, `APPO_API_BASE`
- Exit codes: 0 success, 1 runtime/API error, 2 usage error, 3 confirm required

The USAGE constant in src/cli.mjs (after Plan 01) is the single source for flags + the exit-code/env-var
blocks — transcribe it into README sections. Do NOT document `appo preview` (Phase 4, deferred).

SDK convention sources (verified):
- sdk/llms.txt — the three-part shape: `# <name>` title (line 1), `> ` one-line tagline (line 3), `## Section` blocks of `[label](README.md#anchor)` links.
- sdk/README.md — sectioned/anchorable/neutral voice reference.
The full llms.txt draft for appo is in 06-RESEARCH.md lines 471-505; per-command anchors recommended.

Trusted-publisher runbook facts (06-RESEARCH.md Pitfall 3, lines 335-350; Open Q1, lines 626-636):
- npmjs.com -> package Settings -> Trusted Publisher: provider GitHub Actions; Org/user `appolabs`; Repo `appo`; Workflow filename `release.yml`; Environment blank.
- package.json repository.url must match the GitHub repo exactly.
- npm trusted publishing needs the package to already exist -> document BOTH paths (one manual `npm publish --access public` for v0.1.0, then rely on release.yml for subsequent patch releases). The user chooses; the executor does neither (D-09).
</interfaces>

CLAUDE.md voice rule: README + llms.txt are public-facing — neutral architectural documentation, no
internal strategy phrasing (no "killer feature"/"the bet"/etc.). Rewrite the README in full (delete the
MVP README, do NOT append).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite README.md — full surface, ship-first, with the RELEASING runbook</name>
  <files>README.md</files>
  <read_first>
    - README.md (the MVP README to DELETE and replace — currently login + apps only)
    - src/cli.mjs USAGE (lines 14-70 after Plan 01 — the command list + exit-codes + env-vars blocks)
    - sdk/README.md (structure/voice reference)
    - 06-RESEARCH.md lines 508-527 (the inventory), lines 335-350 + 626-636 (the runbook facts)
    - 06-PATTERNS.md "README.md" (lines 309-331, the required anchor list)
    - CLAUDE.md (neutral public-doc voice)
  </read_first>
  <action>
Replace `README.md` entirely (delete-old-code rule — no append). Structure it with stable anchors that
llms.txt links into. Required sections (each `## ` heading must produce the kebab-case anchor in parens):

1. `# @appolabs/appo` + a one-line tagline.
2. `## Install` (#install) — `npm install -g @appolabs/appo`.
3. `## Ship` (#ship) — the HEADLINE quickstart: install -> `appo init` -> `appo ship --url <u> --name <n>`.
   Show the one-screen story. Mention `appo ship <id>` for an existing app and `--yes` to skip the gate.
4. `## appo init` (#appo-init) — bootstrap config + first login (device or `--token`), idempotent.
5. `## Auth` (#auth) — `login` (device), `login --token <pat>`, `logout`, `whoami`.
6. `## Environments` (#environments) — `env list`, `env use <name>`, the profiles model (no clobber).
7. `## Apps` (#apps) — `apps create --name --url [--meta-name --meta-desc]`, `apps list`, `apps show <id>`, `apps set-name <id> <name>`.
8. One `## ` section per lifecycle verb with its flags + a stable anchor: `## build` (#build),
   `## status` (#status), `## configure` (#configure), `## rejection` (#rejection),
   `## fix-recipe` (#fix-recipe), `## publish` (#publish), `## push` (#push), `## resubmit` (#resubmit).
   Transcribe the exact flags from USAGE. Note the confirm-gate on publish/push/resubmit.
9. `## upgrade` (#upgrade) — `appo upgrade` + `appo --version`/`-v` + the daily update-check notice.
10. `## Environment variables` (#environment-variables) — `APPO_TOKEN`, `APPO_ENV`, `APPO_API_BASE` (transcribe USAGE).
11. `## Exit codes` (#exit-codes) — 0/1/2/3 (transcribe USAGE).
12. `## CI auth` (#ci-auth) — non-interactive: `APPO_TOKEN` env or `appo login --token <pat>` for CI/agents.
13. `## Releasing` (#releasing) — the USER runbook (D-09):
    - The release workflow (`.github/workflows/release.yml`) publishes on push to master/main via OIDC trusted publishing.
    - One-time setup (user-performed): on npmjs.com -> package Settings -> Trusted Publisher -> provider GitHub Actions, org `appolabs`, repo `appo`, workflow filename `release.yml`.
    - First publish: because trusted publishing needs the package to exist, do ONE manual `npm publish --access public` for v0.1.0, then rely on the workflow for subsequent patch releases.
    - State plainly that the CLI tooling never publishes automatically; releasing is a deliberate human action.

Do NOT document `appo preview` (deferred Phase 4). Keep the voice neutral/public.
  </action>
  <verify>
    <automated>npx vitest run test/integration/docs.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - every inventory verb is present: `for v in ship init login logout whoami "env list" "env use" "apps create" "apps list" build status configure rejection fix-recipe publish push resubmit upgrade version; do grep -q "$v" README.md || exit 1; done` exits 0
    - `grep -q "APPO_TOKEN" README.md && grep -q "APPO_ENV" README.md && grep -q "APPO_API_BASE" README.md`
    - `grep -qi "Exit codes" README.md`
    - `grep -qi "Trusted Publisher" README.md` (the RELEASING runbook is present)
    - `grep -q "appo preview" README.md` returns NOTHING (deferred feature not documented)
    - README has >= 120 lines (`wc -l < README.md`)
  </acceptance_criteria>
  <done>
    README.md is rewritten to cover the full command surface with a ship-first quickstart and the
    RELEASING runbook; no preview docs; neutral public voice.
  </done>
</task>

<task type="auto">
  <name>Task 2: llms.txt (SDK shape) + test/integration/docs.test.mjs (command-coverage)</name>
  <files>llms.txt, test/integration/docs.test.mjs</files>
  <read_first>
    - sdk/llms.txt (the exact three-part shape to mirror)
    - 06-RESEARCH.md lines 471-505 (the full appo llms.txt draft to copy)
    - 06-PATTERNS.md "llms.txt" (lines 334-366) and "test files" (lines 402-411)
    - test/integration/help.test.mjs (the iterate-a-command-list + grep analog)
    - README.md (from Task 1 — the anchors llms.txt links into)
  </read_first>
  <behavior>
    - llms.txt starts with `# @appolabs/appo`, has a `> ` tagline line, and `## ` section headers.
    - Every command in the inventory appears as a `[label](README.md#anchor)` link in llms.txt.
    - docs.test.mjs: for each inventory command, assert it appears in BOTH README.md and llms.txt; assert llms.txt matches /^# @appolabs\/appo/m, /^> /m, /^## /m; assert llms.txt contains at least one `README.md#` link.
  </behavior>
  <action>
Create `llms.txt` by copying the draft in 06-RESEARCH.md lines 471-505 (mirrors sdk/llms.txt). Verify the
anchor targets match the README `## ` headings authored in Task 1 (kebab-case). Per-command anchors
recommended (D-07 discretion). Every command in the inventory MUST be linked.

Create `test/integration/docs.test.mjs` (vitest, analog help.test.mjs — iterate a list and grep both
files):
```javascript
import { test, expect } from 'vitest';
import { readFileSync } from 'node:fs';

const README = readFileSync('README.md', 'utf-8');
const LLMS = readFileSync('llms.txt', 'utf-8');

const COMMANDS = [
  'ship','init','login','logout','whoami','env list','env use',
  'apps create','apps list','apps show','apps set-name',
  'build','status','configure','rejection','fix-recipe','publish','push','resubmit',
  'upgrade','version',
];

test.each(COMMANDS)('README documents "%s"', (cmd) => {
  expect(README).toContain(cmd);
});

test.each(COMMANDS)('llms.txt references "%s"', (cmd) => {
  expect(LLMS).toContain(cmd);
});

test('llms.txt matches the SDK shape (title, tagline, sections, README anchors)', () => {
  expect(LLMS).toMatch(/^# @appolabs\/appo/m);
  expect(LLMS).toMatch(/^> /m);
  expect(LLMS).toMatch(/^## /m);
  expect(LLMS).toMatch(/README\.md#/);
});

test('README + llms.txt do not document the deferred preview feature', () => {
  expect(README).not.toContain('appo preview');
  expect(LLMS).not.toContain('appo preview');
});
```
  </action>
  <verify>
    <automated>npx vitest run test/integration/docs.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "^# @appolabs/appo" llms.txt` (title)
    - `grep -q "^> " llms.txt` (tagline)
    - `grep -q "^## " llms.txt` (sections)
    - `grep -q "README.md#" llms.txt` (anchor links)
    - `grep -q "appo ship" llms.txt` and `grep -q "init" llms.txt` and `grep -q "upgrade" llms.txt`
    - `npx vitest run test/integration/docs.test.mjs` is green
  </acceptance_criteria>
  <done>
    llms.txt mirrors the SDK shape and links every command into README anchors; docs.test.mjs asserts
    full command coverage in both files + the llms.txt shape; no preview docs.
  </done>
</task>

<task type="auto">
  <name>Task 3: Phase gate — full suite + clean tarball incl. llms.txt</name>
  <files>(no new product files — verification gate over package.json/README.md/llms.txt)</files>
  <read_first>
    - test/integration/packaging.test.mjs (from Plan 02 — re-run now that llms.txt exists)
    - 06-VALIDATION.md "Validation Sign-Off" (lines 84-93)
    - 06-RESEARCH.md Pitfall 5 (the 9-file tarball, lines 359-369)
  </read_first>
  <action>
This is the Wave-2 phase gate (no new product code). Confirm the whole phase is green now that llms.txt
exists and is in the `files` whitelist (Plan 02 added the slot; this plan landed the file):

1. Run `npm pack --dry-run` and confirm the tarball now lists exactly 9 files:
   bin/appo.mjs, src/api.mjs, src/cli.mjs, src/config.mjs, src/login.mjs, src/ops.mjs, README.md,
   llms.txt, package.json — and nothing from test/.planning/configs/lockfile.
2. Run the full quality gate: `npm test` + `npm run lint` + `npm run typecheck` all exit 0.
3. Confirm `node bin/appo.mjs --version` prints `appo/<v> node/<v>`.
4. Clean up any stray `appolabs-appo-*.tgz` produced by the dry-run (gitignored by Plan 02, but remove it).

If the tarball does NOT include llms.txt, re-check that Plan 02 added `"llms.txt"` to `files` and that the
file exists at the repo root. Do NOT run `npm publish` (D-09).
  </action>
  <verify>
    <automated>npm run lint && npm run typecheck && npm test && npm pack --dry-run 2>&1 | grep -q "llms.txt" && node bin/appo.mjs --version | grep -qE "^appo/[0-9]" && echo GATE_GREEN</automated>
  </verify>
  <acceptance_criteria>
    - `npm run lint` exits 0; `npm run typecheck` exits 0; `npm test` exits 0
    - `npm pack --dry-run 2>&1 | grep -q "llms.txt"` (llms.txt now in the tarball)
    - `npm pack --dry-run 2>&1 | grep -c "total files"` confirms 9 files (no test/.planning/configs)
    - `node bin/appo.mjs --version` prints a line matching `^appo/[0-9]`
    - the command above prints `GATE_GREEN`
    - the executor performed NO `npm publish` and pushed NO tag (D-09)
  </acceptance_criteria>
  <done>
    Full suite + lint + typecheck green; the tarball ships exactly the 9 whitelisted files incl. llms.txt;
    `--version` works; no publish performed.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repository docs -> public readers | README.md + llms.txt ship in the tarball and are public-facing |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-09 | Information Disclosure | README.md / llms.txt | mitigate | Docs MUST NOT embed any secret, real PAT, or real token — examples use placeholders (`<pat>`, `<u>`, `<id>`). The docs.test.mjs and the neutral-voice review keep them example-only; no credential is committed. |
| T-06-10 | Information Disclosure | RELEASING runbook | accept | The runbook names public facts only (npmjs.com settings fields, the GitHub org/repo/workflow filename) — no secret. OIDC trusted publishing means there is no NPM_TOKEN to document or leak. |
| T-06-11 | Elevation (irreversible release) | phase gate | mitigate | The gate (Task 3) stops at `npm pack --dry-run`; it never runs `npm publish` or pushes a tag (D-09). The live release stays the user's documented action. |
</threat_model>

<verification>
- Every inventory command appears in README.md AND llms.txt (docs.test.mjs, iterated).
- llms.txt has the SDK shape (title/tagline/sections) and README anchor links.
- The RELEASING runbook documents the user's one-time trusted-publisher setup + first manual publish (D-09).
- `npm pack --dry-run` now ships 9 files incl. llms.txt; full suite + lint + typecheck green.
- No `appo preview` docs (deferred); no publish performed.
</verification>

<success_criteria>
CLI-05 SC4 is satisfied: README + command reference + llms.txt document every command incl. `appo ship`,
in a neutral public voice, with the releasing runbook. The phase gate is green end to end without a real
publish.
</success_criteria>

<output>
After completion, create `.planning/phases/06-packaging-docs-release/06-03-SUMMARY.md`.
</output>
