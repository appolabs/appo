---
phase: 07-ship-intent-surface
plan: 03
type: execute
wave: 3
depends_on: ["07-01", "07-02"]
files_modified:
  - README.md
  - llms.txt
  - test/integration/docs.test.mjs
autonomous: true
requirements: [SC-4]
nyquist_compliant: true

must_haves:
  truths:
    - "README.md describes ship as create + publish-intent (no build/poll/timeout wording)"
    - "README.md and llms.txt no longer mention --meta-name/--meta-desc/--timeout"
    - "README.md documents `apps update <id> [--name] [--url] [--icon <https-url>]` and `apps create` without --meta-* flags"
    - "docs.test.mjs has negative-assertion guards: README contains no --meta-*/--timeout/build-poll wording, and src/ contains no triggerBuild/pollBuild"
    - "`npm test`, `npm run lint`, and `npm run typecheck` all pass"
  artifacts:
    - path: "README.md"
      provides: "ship (publish-intent) + apps update (icon/url/name) documentation"
      contains: "--icon"
    - path: "llms.txt"
      provides: "command index in lockstep with the rescoped surface"
      contains: "apps update"
    - path: "test/integration/docs.test.mjs"
      provides: "command-inventory greps + negative-assertion regression guards"
      contains: "not.toContain"
  key_links:
    - from: "test/integration/docs.test.mjs"
      to: "README.md + llms.txt + src/cli.mjs + src/ops.mjs"
      via: "readFileSync + positive/negative assertions"
      pattern: "not\\.to(Contain|Match)"
---

<objective>
Bring the docs in lockstep with the rescoped surface and lock the phase with a green
gate. README.md and llms.txt must drop all build/poll/timeout/metadata wording and
describe `ship` as create + publish-intent and `apps update` as name/url/icon. The
existing `docs.test.mjs` greps command NAMES only (so flag-body drift ships silently) —
add negative-assertion guards that fail CI if `--meta-name`/`--meta-desc`/`--timeout`
or build/poll wording reappears in README, and a static guard asserting `src/` has no
`triggerBuild`/`pollBuild`. Then run the full gate: `npm test && npm run lint && npm run typecheck`.

Purpose: Satisfy SC-4 (tests, README, llms.txt updated; full gate green) and lock the
SC-2/SC-3 removals against regression.
Output: Rewritten ship + apps sections in README.md, re-synced llms.txt, augmented
docs.test.mjs, green full gate.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-ship-intent-surface/07-CONTEXT.md
@.planning/phases/07-ship-intent-surface/07-RESEARCH.md
@.planning/phases/07-ship-intent-surface/07-PATTERNS.md
@.planning/phases/07-ship-intent-surface/07-VALIDATION.md

<!-- This plan runs AFTER 07-01 (ship/build-transport) and 07-02 (apps update/icon).
     The src/ surface is already final; this plan only documents it and adds guards.
     CLAUDE.md: "Repository docs read as neutral" — product-documentation voice, no
     strategy/internal framing. -->

<interfaces>
The shipped CLI surface to document (final after 07-01 + 07-02):
- `appo ship --url <u> --name <n> [--stores <list>] [--yes] [--json]` — create + publish-intent (NO --timeout)
- `appo ship <id> [--yes]` — (re)publish-intent on an existing app
- `appo apps create --name <n> --url <u>` — NO --meta-* flags
- `appo apps update <id> [--name <n>] [--url <u>] [--icon <https-url>]` — name/url via PATCH, icon via POST /icon
- ship --json emits `{ steps, final_state }`, final_state in {shipped, gated, blocked}; exit 0 shipped / 1 blocked / 2 usage / 3 gated

docs.test.mjs current shape:
- COMMANDS list (:11-16) — UNCHANGED (`apps update`, `ship`, etc. all stay commands)
- `test.each(COMMANDS)` positive asserts README/llms.txt contain each command name
- Add negative asserts in the same style (mirror the existing `expect(README).toContain` block)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite README.md + llms.txt ship/apps sections (drop build/poll/timeout/metadata)</name>
  <files>README.md, llms.txt</files>
  <read_first>
    - README.md lines 20-50 (ship intro :22-23, quickstart :29-38, ship flag docs :41-49) and 96-125 (apps create :100, apps update :103, apps prose :106-115, status --build :121-124)
    - README.md lines 215-225 (exit-codes prose :219 — "blocked or failed" may simplify to "blocked")
    - llms.txt (FULL — grep ship/publish/push anchors; confirm no --meta-*/--timeout/build-poll flag bodies remain)
    - .planning/phases/07-ship-intent-surface/07-PATTERNS.md (README concrete edit sites list, llms.txt note)
    - .planning/phases/07-ship-intent-surface/07-RESEARCH.md (Pitfall 4 stale-docs)
  </read_first>
  <action>
README.md — apply these edits (line refs from the current file; the wording is the target):

1. Ship intro (:22-23): change
   "...creates an app (when given a URL and name), triggers a build, polls until the
   build is ready, then publishes — stopping..."
   →
   "...creates an app (when given a URL and name), then signals publish-intent —
   Appo issues the build server-side and submits it for you."

2. Quickstart comment (:32): change
   "That single `ship` call runs create → build → poll → publish and streams each step"
   →
   "That single `ship` call runs create → publish-intent and reports the result.
   Appo builds and submits your app; track it with `appo status <id>`."

3. Quickstart line :38 ("# full pipeline, skip the gate"): reword to "# create + ship, skip the publish gate".

4. Ship flag docs (:41-49): change ":42 it rebuilds and republishes" prose as needed,
   and DROP the `--timeout <s>` flag entirely (:44-45 "max seconds to poll a build,
   default 1800"). Keep `--stores`, `--yes`, `--json`. Update the `--json` description
   to "`{steps, final_state}` object" (final_state in {shipped, gated, blocked}).
   Update the exit-code summary (:48-49) from "0 shipped, 1 blocked or failed" to
   "0 shipped, 1 blocked, 2 usage error, 3 gated".

5. apps create (:100): change
   "appo apps create --name <n> --url <u> [--meta-name <m>] [--meta-desc <d>]"
   → "appo apps create --name <n> --url <u>"

6. apps update (:103): change
   "appo apps update <id> [--name <n>] [--url <u>] [--meta-name <m>] [--meta-desc <d>]   # edit app fields"
   → "appo apps update <id> [--name <n>] [--url <u>] [--icon <https-url>]   # edit name, base URL, icon"

7. apps prose (:106-108): drop the "optional store metadata" wording; describe
   `apps update` as editing name, base URL, and the app icon (icon takes an https
   image URL, set via the icon endpoint).

8. The "rebuild and republish" callout (:113-115): reframe to publish-intent voice —
   e.g. "To republish an existing app (or resubmit after a rejection), use `appo ship
   <id>` — Appo rebuilds and resubmits server-side."

9. Exit-codes prose (:219): change "0 shipped, 1 blocked or failed" → "0 shipped, 1 blocked".

Do NOT touch the `appo status <id> --build <buildId>` documentation (:121-124) — build
STATUS reads survive (D-02).

llms.txt — grep for `--meta-name`, `--meta-desc`, `--timeout`, and build/poll wording.
The file is anchor/tagline based (verb anchors for ship/publish/push). Re-verify that
no anchor points at a removed README section and that no removed flag body appears. If
the ship tagline mentions build/poll, reword to publish-intent. Add nothing that
contradicts the README edits.

Keep both docs in neutral product-documentation voice (CLAUDE.md).
  </action>
  <verify>
    <automated>! grep -n "meta-name\|meta-desc\|--timeout" README.md llms.txt && ! grep -niE "create *(→|->) *build *(→|->) *poll" README.md && grep -n "\-\-icon" README.md</automated>
  </verify>
  <acceptance_criteria>
    - `! grep -n "meta-name\|meta-desc\|--timeout" README.md llms.txt` exits 0 (no removed flags)
    - `! grep -niE "build.{0,4}poll|poll.{0,4}publish|polls until" README.md llms.txt` exits 0 (no build/poll wording)
    - `grep -nF "[--icon <https-url>]" README.md` matches (apps update documents --icon)
    - `! grep -nF "[--meta-name" README.md` exits 0 (apps create cleaned)
    - `npx vitest run test/integration/docs.test.mjs` still passes (command-name positive asserts intact)
  </acceptance_criteria>
  <done>
    README.md and llms.txt describe ship as create + publish-intent and apps update as
    name/url/icon, with no --meta-*/--timeout/build-poll wording; the existing
    command-name doc test still passes.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add negative-assertion guards to docs.test.mjs; run the full phase gate</name>
  <files>test/integration/docs.test.mjs</files>
  <read_first>
    - test/integration/docs.test.mjs (FULL — the COMMANDS list :11-16, the README/LLMS readFileSync :8-9, the existing test.each positive asserts)
    - .planning/phases/07-ship-intent-surface/07-PATTERNS.md (docs.test.mjs ADD block — the two negative tests)
    - .planning/phases/07-ship-intent-surface/07-VALIDATION.md (Phase gate command, SC-4 row)
  </read_first>
  <action>
ADD two guard tests to test/integration/docs.test.mjs (mirror the existing style; the
file already does `readFileSync('README.md')` / `readFileSync('llms.txt')`):

    test('README drops removed flags/wording', () => {
      expect(README).not.toContain('--meta-name');
      expect(README).not.toContain('--meta-desc');
      expect(README).not.toContain('--timeout');
      expect(LLMS).not.toContain('--meta-name');
      expect(LLMS).not.toContain('--meta-desc');
      expect(LLMS).not.toContain('--timeout');
    });

    test('src/ has no build-trigger code paths', () => {
      const cli = readFileSync('src/cli.mjs', 'utf-8');
      const ops = readFileSync('src/ops.mjs', 'utf-8');
      expect(cli + ops).not.toMatch(/triggerBuild|pollBuild/);
    });

(`readFileSync` is already imported at the top of docs.test.mjs.)

Then run the full phase gate and ensure all three are green:
  - `npm test`            (full vitest suite — every integration/unit case)
  - `npm run lint`        (eslint --ext .mjs bin/ src/ test/)
  - `npm run typecheck`   (tsc --noEmit, JSDoc-driven)

If lint flags an unused import (e.g. a now-unused helper after the ship.test reshape)
or typecheck flags a JSDoc drift, fix it minimally (delete the unused symbol — no
@ts-ignore, no eslint-disable). Re-run until all three are green.
  </action>
  <verify>
    <automated>npm test && npm run lint && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run test/integration/docs.test.mjs` passes including the two new guard tests
    - The named test "README drops removed flags/wording" exists and is green
    - The named test "src/ has no build-trigger code paths" exists and is green
    - `npm test` exits 0 (full suite green)
    - `npm run lint` exits 0
    - `npm run typecheck` exits 0
    - Static SC-2 guard still holds: `! grep -rn "triggerBuild\|pollBuild" src/` exits 0
  </acceptance_criteria>
  <done>
    docs.test.mjs fails CI if any removed flag/wording reappears or if a build-trigger
    code path returns to src/. The full gate (`npm test && npm run lint && npm run
    typecheck`) is green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Docs/test → CI gate | Documentation and regression guards are the only artifacts; no runtime trust boundary is crossed in this plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-07 | Tampering (regression) | A future change reintroducing `triggerBuild`/`pollBuild` or `--meta-*`/`--timeout` | mitigate | The new `docs.test.mjs` guards fail CI if `src/` regains a build-trigger path or if removed flags reappear in README/llms.txt — a regression lock on the SC-2/SC-3 removals. |
| T-07-08 | Information Disclosure | Internal strategy/voice leaking into committed docs | mitigate | README/llms.txt edits stay in neutral product-documentation voice per CLAUDE.md; no strategy framing, no internal product references. |
</threat_model>

<verification>
- `npm test && npm run lint && npm run typecheck` all green (the phase gate)
- `! grep -n "meta-name\|meta-desc\|--timeout" README.md llms.txt` returns no matches
- `! grep -rn "triggerBuild\|pollBuild" src/` returns no matches
- `npx vitest run test/integration/docs.test.mjs` green including the new guards
</verification>

<success_criteria>
- SC-4: README.md and llms.txt document the rescoped surface (ship = publish-intent;
  apps update = name/url/icon) with no build/poll/timeout/metadata wording; docs.test.mjs
  has negative-assertion guards; `npm test`, `npm run lint`, and `npm run typecheck` all pass.
</success_criteria>

<output>
After completion, create `.planning/phases/07-ship-intent-surface/07-03-SUMMARY.md`
</output>
